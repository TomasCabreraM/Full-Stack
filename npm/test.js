const superheroes = require('superheroes'); // Assuming the module is installed correctly

const randomName = superheroes.random(); // Using consistent variable name

console.log(`This is ${randomName}`); // Using backticks for template literal

